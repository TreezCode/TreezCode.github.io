import { createImageElement } from "./imageElements";
import { showPageInfo, updateButtons } from "./paginationElements";
import { handleMagnify } from "./magnify";
import { renderGallery } from "../gallery";

export default {
    createImageElement,
    showPageInfo,
    updateButtons,
    handleMagnify,
    renderGallery,
};